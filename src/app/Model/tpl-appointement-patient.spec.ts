import { TplAppointementPatient } from './tpl-appointement-patient';

describe('TplAppointementPatient', () => {
  it('should create an instance', () => {
    expect(new TplAppointementPatient()).toBeTruthy();
  });
});
